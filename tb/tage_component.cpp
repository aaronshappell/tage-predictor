#include <verilated.h>

#include "Vtage_component.h"

int main(int argc, char** argv, char** env) {
    // Prevent unused variable warnings
    if (false && argc && argv && env) {}

    // Construct verilated model
    Vtage_component* component = new Vtage_component;

    

    return 0;
}